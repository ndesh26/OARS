<%= form_for(course_completed) do |f| %>
  <% if course_completed.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(course_completed.errors.count, "error") %> prohibited this course_completed from being saved:</h2>

      <ul>
      <% course_completed.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :code %>
    <%= f.text_field :code %>
  </div>

  <div class="field">
    <%= f.label :credit %>
    <%= f.number_field :credit %>
  </div>

  <div class="field">
    <%= f.label :grade %>
    <%= f.text_field :grade %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
