<%= form_for([@course_request.course , @course_request]) do |f| %>
  <% if @course_request.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@course_request.errors.count, "error") %> prohibited this course_request from being saved:</h2>

      <ul>
      <% @course_request.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_id %>
    <%= f.number_field :user_id %>
  </div>

  <div class="field">
    <%= f.label :status %>
    <%= f.text_field :status %>
  </div>

  <div class="field">
    <%= f.label :course_id %>
    <%= f.text_field :course_id %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
